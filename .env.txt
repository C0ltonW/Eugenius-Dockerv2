# ----------------------------
# Project prefix / naming
# ----------------------------
# Compose project name (prefixes containers, network, and named volumes).
COMPOSE_PROJECT_NAME=magedev

# ----------------------------
# Host & ports
# ----------------------------
# Where you'll reach the site (used only when profile=full and nginx is running).
SITE_HOST=127.0.0.1
# Host port that maps to Nginx's port 80 (so you'd visit http://<SITE_HOST>:81/).
APP_PORT=81

# Optional: expose internal service ports to host for tooling/inspection
DB_PORT=3316
SEARCH_PORT=9201
REDIS_PORT=6381

# ----------------------------
# Images
# ----------------------------
# You can point these to your own custom images that include Magento PHP extensions.

# PHP image (must include Magento-required extensions if custom)
PHP_IMAGE=php:8.2-fpm

# Database image: Comment/Uncomment to select the desired image
#DB_IMAGE=mariadb:10.6
DB_IMAGE=mysql:8.0

# Search engine image: Comment/Uncomment to select the desired image
#SEARCH_IMAGE=opensearchproject/opensearch:2.11.0
SEARCH_IMAGE=docker.elastic.co/elasticsearch/elasticsearch:8.15.0

# Web server image
NGINX_IMAGE=nginx:alpine

# Cache image
REDIS_IMAGE=redis:alpine


# ----------------------------
# Database credentials
# ----------------------------
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=magento
MYSQL_USER=magento
MYSQL_PASSWORD=magento

# ----------------------------
# Search JVM heap (OpenSearch)
# ----------------------------
OPENSEARCH_JAVA_OPTS='-Xms512m -Xmx512m'
